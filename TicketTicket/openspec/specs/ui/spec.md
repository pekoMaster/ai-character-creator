# UI/UX 整體規格

## 概述
行動優先的設計，適用於演唱會現場使用情境。

---

## 設計原則

### 行動優先
- 所有介面 SHALL 針對手機螢幕優化
- 觸控目標 SHALL 至少 44x44 像素
- 文字 SHALL 清晰可讀（最小 16px）

### 可信度
- 使用清晰的標籤和圖示
- 重要資訊使用醒目色彩
- 提供充足的視覺回饋

### 效率
- 減少操作步驟
- 提供快速存取常用功能
- 支援手勢操作

---

## 色彩系統

### 主色調
```css
--primary: #6366f1;      /* 靛藍色 - 主要動作 */
--primary-hover: #4f46e5;
--primary-light: #e0e7ff;
```

### 狀態色彩
```css
--success: #22c55e;      /* 綠色 - 成功、確認 */
--warning: #f59e0b;      /* 橙色 - 警告 */
--error: #ef4444;        /* 紅色 - 錯誤 */
--info: #3b82f6;         /* 藍色 - 資訊 */
```

### 中性色
```css
--gray-50: #f9fafb;
--gray-100: #f3f4f6;
--gray-200: #e5e7eb;
--gray-300: #d1d5db;
--gray-400: #9ca3af;
--gray-500: #6b7280;
--gray-600: #4b5563;
--gray-700: #374151;
--gray-800: #1f2937;
--gray-900: #111827;
```

### 票券類型標籤色彩
```css
--tag-must-enter: #8b5cf6;   /* 紫色 - 需同行入場 */
--tag-overseas: #f59e0b;      /* 橙色 - 海外票券 */
--tag-separate: #22c55e;      /* 綠色 - 獨立連結 */
```

---

## 排版系統

### 字體
```css
font-family: 'Inter', 'Noto Sans TC', system-ui, sans-serif;
```

### 字級
```css
--text-xs: 0.75rem;    /* 12px */
--text-sm: 0.875rem;   /* 14px */
--text-base: 1rem;     /* 16px */
--text-lg: 1.125rem;   /* 18px */
--text-xl: 1.25rem;    /* 20px */
--text-2xl: 1.5rem;    /* 24px */
--text-3xl: 1.875rem;  /* 30px */
```

### 字重
```css
--font-normal: 400;
--font-medium: 500;
--font-semibold: 600;
--font-bold: 700;
```

---

## 間距系統

```css
--space-1: 0.25rem;   /* 4px */
--space-2: 0.5rem;    /* 8px */
--space-3: 0.75rem;   /* 12px */
--space-4: 1rem;      /* 16px */
--space-5: 1.25rem;   /* 20px */
--space-6: 1.5rem;    /* 24px */
--space-8: 2rem;      /* 32px */
--space-10: 2.5rem;   /* 40px */
--space-12: 3rem;     /* 48px */
```

---

## 元件規格

### 按鈕
```
主要按鈕 (Primary)
┌─────────────────────────────┐
│     [bg: primary, 白字]     │
│     padding: 12px 24px      │
│     border-radius: 8px      │
└─────────────────────────────┘

次要按鈕 (Secondary)
┌─────────────────────────────┐
│     [bg: white, 邊框]       │
│     border: 1px gray-300    │
└─────────────────────────────┘

危險按鈕 (Danger)
┌─────────────────────────────┐
│     [bg: error, 白字]       │
└─────────────────────────────┘
```

### 卡片
```
┌─────────────────────────────┐
│  padding: 16px              │
│  border-radius: 12px        │
│  box-shadow: sm             │
│  background: white          │
└─────────────────────────────┘
```

### 標籤 (Tag)
```
┌──────────────┐
│ [文字]       │  padding: 4px 12px
│              │  border-radius: 9999px
└──────────────┘  font-size: 12px
```

### 輸入框
```
┌─────────────────────────────┐
│  padding: 12px 16px         │
│  border: 1px gray-300       │
│  border-radius: 8px         │
│  focus: ring-2 primary      │
└─────────────────────────────┘
```

---

## 頁面結構

### 底部導覽列
```
┌───────────────────────────────────────┐
│                                       │
│   🏠      🔍      ➕      💬      👤   │
│   首頁    探索    發布    訊息    我的  │
│                                       │
└───────────────────────────────────────┘

高度: 64px
背景: white
邊框: top 1px gray-200
```

### 頂部標題列
```
┌───────────────────────────────────────┐
│  ←  頁面標題                    [動作] │
└───────────────────────────────────────┘

高度: 56px
背景: white
邊框: bottom 1px gray-200
```

---

## 響應式設計

### 斷點
```css
--breakpoint-sm: 640px;
--breakpoint-md: 768px;
--breakpoint-lg: 1024px;
```

### 行動版優先
- 預設樣式針對 <640px
- 使用 @media min-width 增加大螢幕樣式

---

## 動畫與過渡

### 過渡時間
```css
--duration-fast: 150ms;
--duration-normal: 200ms;
--duration-slow: 300ms;
```

### 緩動函數
```css
--ease-default: cubic-bezier(0.4, 0, 0.2, 1);
--ease-in: cubic-bezier(0.4, 0, 1, 1);
--ease-out: cubic-bezier(0, 0, 0.2, 1);
```

### 使用時機
- 按鈕懸停：fast
- Modal 開關：normal
- 頁面切換：slow
